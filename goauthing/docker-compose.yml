name: goauthing

services:
  goauthing:
    image: git.s.thusaac.com:12711/shanjb0221/goauthing:latest
    container_name: goauthing
    restart: unless-stopped
    volumes:
      - ./config:/.auth-thu
    command: auth -k
